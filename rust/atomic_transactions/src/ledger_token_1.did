service : {
    "init" : (vec nat, vec nat) -> ();
    "prepare_transaction" : (nat, nat, nat) -> (bool);
    "abort_transaction" : (nat, nat) -> ();
    "commit_transaction" : (nat, nat, nat) -> ();
};
